{
  "name": "codex-realtime-21-voice",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node server.mjs",
    "dev": "node --watch server.mjs",
    "auth": "OPENCLAW_STATE_DIR=.openclaw-state openclaw models auth login --provider openai --profile-id openai:realtime-21",
    "xspace:install": "bash scripts/install-xspace-deps.sh",
    "test": "node --test tests/*.test.mjs",
    "check": "node --check server.mjs && node --check codex-app-server.mjs && node --check xspace-source.mjs && node --check public/app.js && python3 -m py_compile xspace/xspace_listener.py && npm test"
  },
  "dependencies": {
    "openclaw": "2026.8.1-beta.2",
    "ws": "^8.18.3"
  }
}
